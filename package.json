{
  "name": "yapper",
  "version": "1.0.0",
  "description": "Token Compression & Prompt Translation Engine",
  "scripts": {
    "build": "bun build js/app.js --outfile app.js --target browser --minify",
    "dev": "bun build js/app.js --outfile app.js --target browser --watch"
  },
  "devDependencies": {}
}
